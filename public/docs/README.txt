Per-property compliance documents (appended to every tenancy pack).

- Put each property's current EPC, Gas Safety certificate and EICR (as PDFs)
  into public/docs/<property-key>/  — see /admin/tenancy-docs for each key.
- Put the shared "A Home in the Private Rented Sector: A Guide for Tenants in
  Wales" PDF into public/docs/_shared/  (it goes on every pack).
